{
	"name": "yt-summarizer",
	"version": "0.1.0",
	"description": "Summarize a YouTube video's transcript into a new note using Gemini.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "^1.5.7",
		"typescript": "^5.4.0"
	}
}